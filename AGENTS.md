# AI agent entry point

This root file exists only for tools that discover `AGENTS.md`.

Read and follow [`2.core/AGENTS.md`](2.core/AGENTS.md). Its shared authority is [`2.core/CONTRACT.md`](2.core/CONTRACT.md). Provider adapters under `1.plugins/` and optional tools under `3.add-ons/` cannot override that contract.

Do not add architecture rules to this compatibility pointer.
