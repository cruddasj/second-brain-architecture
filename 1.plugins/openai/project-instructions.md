# Optional OpenAI project adapter

This file is the AI-agent adapter for an OpenAI connection. For general orientation, use [README.md](README.md).

Before use, replace `OWNER/REPOSITORY` with the private repository that holds the user's second brain.

Use the connected repository `OWNER/REPOSITORY` on its canonical default branch.

Read and follow `2.core/CONTRACT.md` as the complete operating contract. Treat the repository's canonical default branch as the only authoritative saved second brain. Provider-managed memory may be a secondary convenience but cannot replace a completed repository transaction.

Use only the repository and tools authorised for the task. Do not write to another repository. If repository access or canonical write-back fails, report the failure and do not claim that information was saved.
